package basic

fun main(){

    /**
     * true ya da false deger ataması için kullanılır
     * 0 ya da 1 Boolean olarak kullanılmaz
     */

    val isStudent : Boolean = true
    val isTeacher : Boolean = false

    /**
     *
     * && , || , xor , nor kullanımlar mevcut
     *
     *  Boolean degerinin true olmasını kontrol etmek için ==
     * kullanmamıza gerek yoktur.
     *
     *  Zaten true ise suslu parantezler icerisine girecektir
     */
    if (isStudent == true){
        //Yanlıs kullanım
    }
    if (isStudent){
        //Dogru kullanım
    }

    // false için de == false gibi bir şey yazmamıza gerek yoktur.








}